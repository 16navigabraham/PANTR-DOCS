{
  "framework": null,
  "buildCommand": "(git rev-parse --is-shallow-repository | grep -q true && git fetch --unshallow || echo 'already full clone') && echo 'git depth OK' && npx next build && npx pagefind --site out --output-path out/_pagefind",
  "outputDirectory": "out",
  "cleanUrls": true,
  "trailingSlash": false
}
