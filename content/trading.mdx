---
title: Trading Engine
---

# Trading Engine

The `TradeOrchestrator` (`src/services/trade-orchestrator.js`) is the central trading engine. All buys, sells, and swaps flow through it.

## Pre-Trade Confirmation

Before executing ANY buy, sell, swap, or withdrawal, Claude must:

1. Call `check_balance` to get the user's current balance
2. Estimate gas cost (Solana ~0.000005 SOL, Base ~$0.01-0.05 in ETH)
3. For sells: calculate 2% close fee from the expected output
4. Present a summary and wait for user confirmation before calling the trade tool

Examples:
- Buy: "Buying $30 of BONK + ~$0.01 gas. Confirm?"
- Sell: "Selling 1.5M BONK (~$44). After 2% fee ($0.88), you'll receive ~$43.12. Confirm?"

Exceptions (no confirmation needed): auto-sell triggers, alpha auto-buys, and info queries.

## Buy Flow

```
User: "buy $10 of DezXAZ..."
         |
         v
   Claude: check_balance → estimate gas → show summary (no platform fee on entry)
         |
         v
   User confirms → Tool Executor → orchestrator.buyToken()
         |
         v
   1. Validate user + wallet
   2. Safety check (GoPlus/RugCheck)
   3. Create SUBMITTED trade record in DB
   4. Route to chain-specific swap:
      - Solana → Jupiter V4 API
      - Base → Odos → KyberSwap → Uniswap V3 → Uniswap V4
   5. Full amount swapped — no fee deducted on buy
   6. On success: mark CONFIRMED, store txHash + amounts
   7. On failure: mark FAILED, store error
   8. Notify user via Telegram
```

## Validations

- User exists and is active
- Wallet exists for the target chain
- Amount >= $2 minimum
- Token passes safety check (risk score ≤ user's `maxRiskScore`)
- Honeypot tokens blocked entirely
- Known stablecoins (USDC) skip safety checks
- `minLiquidityUsd` user setting passed through to safety checks

## USD to Native Conversion

When user says "buy $10", the amount is in USD:

- **Solana**: fetch SOL price → `$10 / solPrice = amountInSol`
- **Base**: swap service handles USD→ETH conversion internally

## Sell Flow

```
User: "sell 50% of my BONK"
         |
         v
   1. Look up user's on-chain token balance
   2. Calculate: balance * 50 / 100
   3. Create SUBMITTED sell trade in DB
   4. Execute swap: token → SOL/ETH
   5. Calculate P&L (sell price vs cost basis)
   6. Mark CONFIRMED, notify user
```

## Swap Routing

### Solana

Primary router: **Jupiter V4 API**

```
getQuote() → buildSwapTransaction() → sign via Privy → sendTransaction() → confirm
```

Pump.fun tokens follow a multi-stage routing cascade:

```
isPumpFunMint(address)?
  YES → detectBondingCurve() →
        active?  YES → pumpswap-direct.executeBuy()   (direct program call)
                          ↓ fail
                       pumpfun-swap.js via PumpPortal  (bonding curve fallback)
                          ↓ fail
                       Jupiter (last resort)
        active?  NO  → Jupiter DEX
                          ↓ NO_ROUTES_FOUND + couldBePump?
                       pumpfun-swap.js via PumpPortal  (graduated token AMM, ~5 min Jupiter lag)
  NO  → Jupiter DEX
```

- **Bonding curve tokens**: `pumpswap-direct.js` hits the Pump.fun program directly (no PumpPortal round-trip). Falls back to PumpPortal, then Jupiter.
- **Graduated tokens** (post-bonding curve): Jupiter is tried first. If `NO_ROUTES_FOUND`, PumpPortal fallback routes via PumpSwap AMM directly — bypassing Jupiter's ~5 minute indexing lag after graduation.

### Base (EVM)

Cascading fallback with multiple DEX aggregators:

```
1. Odos         → if fail/no route →
2. KyberSwap    → if fail/no route →
3. Uniswap V3   → if fail/no route →
4. Uniswap V4
```

Each aggregator:

1. Get quote (best price + route)
2. Build transaction
3. Sign via Privy EVM signer
4. Submit and wait for receipt

## Fees

| Event | Fee |
|-------|-----|
| Buy (manual or alpha auto-buy) | **0%** — no platform fee on entry |
| Close (manual sell, TP trigger, SL trigger) | **2%** taken from native token received (SOL or ETH) |

Fee is taken from the output amount after the swap:

- User sells tokens → receives 1.0 SOL → 0.02 SOL fee → user gets 0.98 SOL
- Fee transferred to treasury wallet in the same transaction

Fee collection: `src/services/fee-collector.js`

## Buy Speed (Priority Fee Tiers)

On Solana, the **priority fee** (compute-unit price) determines how fast a buy lands —
a higher fee bids your transaction ahead in the queue. This is landing **speed**, and
is orthogonal to **slippage** (price tolerance); the two are set independently.

Pantr picks a **congestion-aware dynamic fee** by default — it reads recent network
prioritization fees (75th percentile × a multiplier, clamped) so buys stay competitive
without overpaying in calm conditions. Traders can bias this faster per their style:

| Tier | Fee | Use |
|---|---|---|
| **Auto** (default) | dynamic, congestion-aware | smart default for everyone |
| **Fast** | dynamic × 1.6 | land ahead of the Auto crowd |
| **Turbo** | dynamic × 2.5 | chasing a hot alpha — front of queue |
| **Custom** | exact value (µLam/CU) | full manual control |

Set via chat: *"set my buy speed to turbo"* → `update_settings(priorityFeeTier='turbo')`.
Stored on `UserSettings.priorityFeeTier` (+ `priorityFeeCustomMicroLamports` for custom).
Costs stay trivial — even Turbo is a fraction of a cent per buy. Alpha (trenches) buys
also carry a higher floor so they stay aggressive even when the network is quiet.

Resolution lives in `solana-swap.js` (`getTieredPriorityFee`) and is threaded through
`trade-orchestrator.js` into every Solana buy route.

## Gas Costs

Every on-chain transaction costs network gas, separate from the platform fee:

- **Solana**: ~0.000005 SOL per transaction
- **Base**: ~$0.01-0.05 in ETH per transaction

Base trades always require ETH for gas regardless of what token is being traded. Users need a small ETH balance to cover gas on Base.

## Stuck Trade Resolution

Background job runs every 5 minutes:

1. Find trades in PENDING/SUBMITTED status for >10 minutes
2. Query the blockchain for transaction status
3. If confirmed on-chain → mark CONFIRMED
4. If failed/dropped → mark FAILED
5. If still pending → leave for next check

## Trade Queue

`src/lib/trade-queue.js` — limits concurrent trade execution to 3.

Prevents:
- Nonce conflicts on EVM
- RPC rate limiting
- Database connection exhaustion

Trades queue up and execute in order. Stats logged every 60 seconds.

## Swap Services

| Service | Chains | DEXes |
|---------|--------|-------|
| `solana-swap.js` | Solana | Jupiter V4 |
| `pumpfun-swap.js` | Solana | Pump.fun bonding curve |
| `evm-swap.js` | Base | Odos, KyberSwap, Uniswap V3, Uniswap V4 |

## Portfolio

`check_portfolio` aggregates the user's open positions with current prices and P&L.

### Spam Token Filtering

Airdropped spam tokens (e.g. PreSPAX) are common on Base and Solana. The portfolio tool filters these out:

- Tokens with `isSpam: true` in the trade record are excluded from results
- The `spamFiltered` count is returned so Claude can mention it if relevant
- The system prompt instructs Claude to never display spam tokens
